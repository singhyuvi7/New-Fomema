<div class="card">
    <div class="card-header border-0 bg-transparent text-dark">
        <strong>Register Employer</strong> - New Employer 
    </div>
        <%= form_for @employer, {url: external_agency_employers_path, html: { multipart: true, class: "with-parsley employer-form" }} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>
