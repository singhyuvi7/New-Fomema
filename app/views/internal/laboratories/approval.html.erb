<div class="card">
    <div class="card-header">
        <strong>Laboratories</strong> - Approval
    </div>
    <%= form_for @laboratory, url: approval_update_internal_laboratories_path(@laboratory), html: {class: "#{params[:action]}_laboratory laboratory-form"} do |form| %>

        <div class="card-body">
            <%= render 'form_readonly', { form: form } %>

            <div class="card">
                <div class="card-body">

                    <%=render "shared/approval_comments", { comments: @laboratory.approval_request.comments } if @laboratory.approval_request %>

                    <%= render 'internal/shared/approval_comment_decision', form: form, can_revert: true %>
                </div>
            </div>
        </div>

        <div class="card-footer">

            <div class="text-right">
                <%=raw edit_back_button(internal_laboratories_path) %>
                <%=raw approval_submit_decision_button %>
            </div>
        
        </div>
    <% end %>
</div>