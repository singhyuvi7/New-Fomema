<div class="card">
    <div class="card-header">
        <strong>Laboratories</strong> - New Laboratory
    </div>
    <%= form_for @laboratory, url: internal_laboratories_path, html: {class: "#{params[:action]}_laboratory laboratory-form with-parsley disable-enter-keypress"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>