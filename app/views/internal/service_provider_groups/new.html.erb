<div class="card">
    <div class="card-header">
        <strong>Service Provider Groups</strong> - New Service Provider Group
    </div>
    <%= form_for @service_provider_group, url: internal_service_provider_groups_path, html: {class: "#{params[:action]}_service_provider_group service_provider_group-form with-parsley disable-enter-keypress"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>