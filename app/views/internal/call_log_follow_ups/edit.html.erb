<div class="card">
    <div class="card-header">
        <strong>Follow Ups</strong> - Edit Follow Up
    </div>
    <%= form_for @call_log_follow_up, url: internal_call_log_call_log_follow_up_path(@call_log, @call_log_follow_up), html: {class: "#{params[:action]}_call_log call_log-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>