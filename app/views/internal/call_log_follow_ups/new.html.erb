<div class="card">
    <div class="card-header">
        <strong>Follow Ups</strong> - New Follow Up
    </div>
    <%= form_for @call_log_follow_up, url: internal_call_log_call_log_follow_ups_path(@call_log), html: {class: "with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>