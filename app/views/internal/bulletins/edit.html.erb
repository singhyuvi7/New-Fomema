<div class="card">
    <div class="card-header">
        <strong>Bulletins</strong> - Edit Bulletin
    </div>
    <%= form_for @bulletin, {url: internal_bulletin_path(@bulletin), html: {multipart: true, class: "with-parsley"}} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>

    <%=render partial: "filter_doctor_audience" %>
    <%=render partial: "filter_laboratory_audience" %>
    <%=render partial: "filter_xray_facility_audience" %>
    <%=render partial: "filter_employer_audience" %>
    <%=render partial: "filter_agency_audience" %>
</div>