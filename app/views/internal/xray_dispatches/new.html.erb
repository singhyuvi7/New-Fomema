<div class="card">
    <div class="card-header">
        <strong>X-Ray Dispatches</strong> - New X-Ray Dispatch
    </div>

    <%= form_for @xray_dispatch, url: internal_xray_dispatches_path, html: { class: "with-parsley" } do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>