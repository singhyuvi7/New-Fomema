<div class="card">

    <div class="card-header">
        <strong>Countries</strong> - Edit Country
    </div>
    <%= form_for @country, url: internal_country_path(@country), html: {class: "#{params[:action]}_country country-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
    
</div>