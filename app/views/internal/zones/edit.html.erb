<div class="card">

    <div class="card-header">
        <strong>Zones</strong> - Edit Zone
    </div>
    <%= form_for @zone, url: internal_zone_path(@zone), html: {class: "#{params[:action]}_zone zone-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
    
</div>