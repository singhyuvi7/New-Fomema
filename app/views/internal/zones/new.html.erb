<div class="card">
    <div class="card-header">
        <strong>Zones</strong> - New Zone
    </div>
    <%= form_for @zone, url: internal_zones_path, html: {class: "#{params[:action]}_zone zone-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>