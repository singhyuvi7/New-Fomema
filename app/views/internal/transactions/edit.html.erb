<div class="card">
    <div class="card-header">
        <strong>Transactions</strong> - Edit Transaction
    </div>
    <%= form_for @transaction, url: internal_transaction_path(@transaction), html: {class: "#{params[:action]}_doctor doctor-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>