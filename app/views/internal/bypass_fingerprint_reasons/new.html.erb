<div class="card">
    <div class="card-header">
        <strong>Bypass Fingerprint Reasons</strong> - New Bypass Fingerprint Reason
    </div>
    <%= form_for @bypass_fingerprint_reason, url: internal_bypass_fingerprint_reasons_path, html: {class: "#{params[:action]}_bypass_fingerprint_reason bypass_fingerprint_reason-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>