<div class="card">
    <div class="card-header"><strong>Users</strong> - Editing User</div>
    <%= nested_form_for @user, {url: internal_employer_employer_user_path(@employer, @user), html: {multipart: true, class: "with-parsley"}} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>