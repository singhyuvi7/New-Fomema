<div class="card">
    <div class="card-header"><strong>Users</strong> - New User</div>
    <%= nested_form_for @user, {url: internal_employer_employer_users_path(@employer), html: {multipart: true, class: "with-parsley"}} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>
 