<div class="card">
    <div class="card-header">
        <strong>Block Reasons</strong> - Edit Block Reason
    </div>
    <%= form_for @block_reason, url: internal_block_reason_path(@block_reason), html: {class: "#{params[:action]}_block_reason block_reason-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>