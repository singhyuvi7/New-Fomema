<table class="workers-table w-100" style="margin-top: 2rem;">
    <thead>
        <tr>
            <% @headers.each do |header| %>
            <th><%= header %></th>
            <% end %>
        </tr>
    </thead>
    <tbody valign="top">
        <% @laboratories.each.with_index(1) do |laboratory,index| %>
        <tr>
            <td><%= index %></td>
            <td>
                <div class="service-provider-details"><%= laboratory.code %></div>
            </td>
            <td>
                <div class="service-provider-details">
                    <div><%= laboratory.name %></div>
                    <div><%= laboratory.address1 %></div>
                    <div><%= laboratory.address2 %></div>
                    <div><%= laboratory.address3 %></div>
                    <div><%= laboratory.address4 %></div>
                    <div><%= laboratory.town.try(:name) %></div>
                    <div><%= laboratory.postcode %> <%= laboratory.state.try(:name) %></div>
                </div>
            </td>
            <td class="multi-line">
                <div><%= laboratory.created_at.try(:strftime, get_standard_date_format) %></div>
            </td>
            <td class="multi-line">
                <div><%= laboratory.activated_at.try(:strftime, get_standard_date_format) %></div>
            </td>
        </tr>
        <% end %>
    </tbody>
</table>

<style>
    .w-100 {
        width: 100%;
    }

    table {
        border-collapse: collapse;
        border: 1px solid #aaa;
    }

    .workers-table {
        padding: 1rem 0px;
    }

    thead {
        background-color: #d0d0d0;
    }

    thead th {
        padding: 3px;
        border: 1px solid #aaa;
    }

    tbody td {
        padding: 5px 5px;
        vertical-align: center;
        border: none;
        border-bottom: 1px solid #aaa;
    }

    .service-provider-details {
        height: 170px;
    }

    .paired-service-provider div,
    .multi-line div {
        padding: 5px 0px;
    }
</style>