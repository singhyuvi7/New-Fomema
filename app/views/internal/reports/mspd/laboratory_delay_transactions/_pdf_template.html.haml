.table-responsive
  %table.table.w-100.text-center
    %thead
      %tr
        - headers.each do |header|
          %th= header
    %tbody
      - if transactions.present?
        - transactions.each.with_index(1) do |transaction|
          %tr
            %td= transaction.fw_name
            %td= transaction.fw_code
            %td= transaction.fw_passport_number
            %td= transaction.doctor_code
            %td= transaction.laboratory_code
            %td= transaction.laboratory_name
            %td= transaction.laboratory_town_name
            %td= transaction.laboratory_state_name
            %td= transaction.medical_examination_date&.strftime('%d/%m/%Y')
            %td= transaction.specimen_taken_date&.strftime('%d/%m/%Y')
            %td= transaction.specimen_received_date&.strftime('%d/%m/%Y')
            %td= transaction.laboratory_transmit_date&.strftime('%d/%m/%Y')
            %td= transaction.lab_submit_day_taken

:css
  .w-100 {
      width: 100%;
  }

  table {
      border-collapse: collapse;
      border: 1px solid #aaa;
  }

  thead {
      background-color: #d0d0d0;
  }

  thead th {
      padding: 10px 3px;
      border: 1px solid #aaa;
  }

  tbody td {
      padding: 10px 5px;
      vertical-align: center;
      border: 1px solid #aaa;
  }

  .bold {
      font-weight: bold;
  }
