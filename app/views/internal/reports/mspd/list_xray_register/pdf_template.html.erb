<table class="workers-table w-100" style="margin-top: 2rem;">
    <thead>
        <tr>
            <% @headers.each do |header| %>
            <th><%= header %></th>
            <% end %>
        </tr>
    </thead>
    <tbody valign="top">
        <% @xray_facilities.each.with_index(1) do |xray_facility,index| %>
        <tr>
            <td><%= index %></td>
            <td>
                <div class="service-provider-details"><%= xray_facility.code %></div>
            </td>
            <td>
                <div class="service-provider-details">
                    <div><%= xray_facility.name %></div>
                    <div><%= xray_facility.license_holder_name %></div>
                    <div><%= xray_facility.address1 %></div>
                    <div><%= xray_facility.address2 %></div>
                    <div><%= xray_facility.address3 %></div>
                    <div><%= xray_facility.address4 %></div>
                    <div><%= xray_facility.town.try(:name) %></div>
                    <div><%= xray_facility.postcode %> <%= xray_facility.state.try(:name) %></div>
                </div>
            </td>
            <td class="multi-line">
                <div><%= xray_facility.created_at.try(:strftime, get_standard_date_format) %></div>
            </td>
            <td class="multi-line">
                <div><%= xray_facility.activated_at.try(:strftime, get_standard_date_format) %></div>
            </td>
        </tr>
        <% end %>
    </tbody>
</table>

<style>
    .w-100 {
        width: 100%;
    }

    table {
        border-collapse: collapse;
        border: 1px solid #aaa;
    }

    .workers-table {
        padding: 1rem 0px;
    }

    thead {
        background-color: #d0d0d0;
    }

    thead th {
        padding: 3px;
        border: 1px solid #aaa;
    }

    tbody td {
        padding: 5px 5px;
        vertical-align: center;
        border: none;
        border-bottom: 1px solid #aaa;
    }

    .service-provider-details {
        height: 170px;
    }

    .paired-service-provider div,
    .multi-line div {
        padding: 5px 0px;
    }
</style>