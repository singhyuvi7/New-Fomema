<!-- new template for index -->

<div class="card">
    <div class="card-header">
        <strong>Visited Statistic</strong>
    </div>

    <!-- card-body -->
    <div class="card-body">

        <%= render "/shared/all_flash" %>
        <%=render 'internal/reports/inspectorate/shared/filter', { filter_link: visited_statistic_internal_inspectorate_reports_path} %>
    </div>
    <!-- /card-body -->
</div>

<% content_for :page_end_scripts do %>
<script>
    $('.with-excel-format').click(function (e) {
        $(this).attr("href", this.href + ".xlsx?" + $(this).closest('form').serialize());
    });
</script>
<% end %>