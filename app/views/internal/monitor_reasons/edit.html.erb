<div class="card">
    <div class="card-header">
        <strong>Monitor Reasons</strong> - Edit Monitor Reason
    </div>
    <%= form_for @monitor_reason, url: internal_monitor_reason_path(@monitor_reason), html: {class: "#{params[:action]}_monitor_reason monitor_reason-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>