<div class="card">
    <div class="card-header">
        <strong>Amendment Reasons</strong> - New Amendment Reason
    </div>
    <%= form_for @amendment_reason, url: internal_amendment_reasons_path, html: {class: "#{params[:action]}_amendment_reason amendment_reason-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>