<div class="card">
    <div class="card-header">
        <strong>Towns</strong> - Edit Town
    </div>
    <%= form_for @town, url: internal_town_path(@town), html: {class: "#{params[:action]}_town town-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>