<div class="card">
    <div class="card-header">
        <strong>Approval Approvers</strong> - Edit Approval Approver
    </div>
    <%= form_for @approval_approver, url: internal_approval_approver_path(@approval_approver), html: {class: "#{params[:action]}_approval approval-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>