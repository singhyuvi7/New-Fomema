<div class="card">
    <div class="card-header">
        <strong>Approval Approvers</strong> - New Approval Approver
    </div>
    <%= form_for @approval_approver, url: internal_approval_approvers_path, html: {class: "#{params[:action]}_approval_approver approval-approver-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>