<div class="card">
    <div class="card-header">
        <strong>Clinics</strong> - New Clinic
    </div>
    <%= form_for @clinic, url: internal_clinics_path do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>