<div class="card">
    <div class="card-header">
        <strong>X-Ray Storages</strong> - Edit X-Ray Storage
    </div>

    <%= nested_form_for @xray_storage, url: internal_xray_storage_path(@xray_storage) do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>