<div class="card">
    <div class="card-header">
        <strong>Races</strong> - New Race
    </div>
    <%= form_for @race, url: internal_races_path, html: {class: "#{params[:action]}_race race-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>