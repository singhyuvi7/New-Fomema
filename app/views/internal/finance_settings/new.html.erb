<div class="card">
    <div class="card-header">
        <strong>Finance Settings</strong> - New Finance Setting
    </div>
    <%= form_for @finance_setting, url: internal_finance_settings_path, html: {class: "#{params[:action]}_fee fee-form with-parsley"} do |form| %>
    <%= render 'form', { form: form } %>
    <% end %>
</div>