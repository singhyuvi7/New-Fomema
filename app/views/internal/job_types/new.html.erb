<div class="card">
    <div class="card-header">
        <strong>Job Types</strong> - New Job Type
    </div>
    <%= form_for @job_type, url: internal_job_types_path, html: {class: "#{params[:action]}_job_type job_type-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>