<tr>
    <td><%= "#{order_item.fee.name} #{"(#{order_item.insurance_purchase.insurance_provider})" if order_item.insurance_purchase}" %></td>
    <td><%= "#{order_item.order_itemable.name}" %></td>
    <td class="currency"><%= order_item.insurance_purchase&.total_premium ? number_to_currency(order_item.insurance_purchase&.total_premium, unit: "") : '-' %></td>
    <td class="order-item-actions">
        <input type="hidden" name="foreign_worker_ids[]" value="<%= order_item.order_itemable_id %>">
        <button type="button" class="btn btn-sm btn-secondary" onclick="etr_remove(this)">X</button>
    </td>
</tr>