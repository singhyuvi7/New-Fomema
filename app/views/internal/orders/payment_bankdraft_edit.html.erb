<div class="card">
    <div class="card-body">
        <div class="card">
            <div class="card-header">
                New Bank Draft
            </div>
            <%= form_for @bank_draft, url: payment_bankdraft_update_internal_order_path(@order, @bank_draft), html: {class: "with-parsley"}, method: :patch do |form| %>
                <div class="card-body">
                    <%=render 'payment_bankdraft_form', { form: form } %>
                </div>
                <!-- /.card-body -->

                <div class="card-footer">
                
                    <div class="text-right">
                        <%=raw edit_back_button(payment_internal_order_path(@order)) %>
                        <%=raw edit_save_button() %>
                    </div>
                
                </div>
            <% end %>
        </div>
    </div>
</div>