<div class="card">
    <div class="card-header">
        <strong>Titles</strong> - New Title
    </div>
    <%= form_for @title, url: internal_titles_path, html: {class: "#{params[:action]}_title title-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>