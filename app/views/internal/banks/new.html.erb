<div class="card">
    <div class="card-header">
        <strong>Banks</strong> - New Bank
    </div>
    <%= form_for @bank, url: internal_banks_path, html: {class: "#{params[:action]}_bank bank-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>