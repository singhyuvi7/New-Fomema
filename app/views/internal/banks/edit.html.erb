<div class="card">
    <div class="card-header">
        <strong>Banks</strong> - Edit Bank
    </div>
    <%= form_for @bank, url: internal_bank_path(@bank), html: {class: "#{params[:action]}_bank bank-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>