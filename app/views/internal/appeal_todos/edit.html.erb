<div class="card">
    <div class="card-header">
        <strong>Appeal Todos</strong> - Edit Appeal Todo
    </div>
    <%= form_for @appeal_todo, url: internal_appeal_todo_path(@appeal_todo), html: {class: "#{params[:action]}_appeal_todo appeal_todo-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>