<div class="card">
    <div class="card-header">
        <strong>Retake Reasons</strong> - New Retake Reason
    </div>
    <%= form_for @retake_reason, url: internal_retake_reasons_path, html: {class: "#{params[:action]}_retake_reason retake_reason-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>