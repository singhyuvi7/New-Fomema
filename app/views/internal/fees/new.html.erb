<div class="card">
    <div class="card-header">
        <strong>Fees</strong> - New Fee
    </div>
    <%= form_for @fee, url: internal_fees_path, html: {class: "#{params[:action]}_fee fee-form with-parsley"} do |form| %>
        <%= render 'form', { form: form } %>
    <% end %>
</div>